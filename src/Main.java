import support.Checker;
import support.Message;
import tokenRing.TokenRingImpl;

public class Main {
    public static void main(String[] args) throws InterruptedException {
        Checker checker = new Checker();
        checker.check();
    }
}
